# Shiny Data Bundles

This folder stores precomputed regional `.rds` bundles for the Shiny app.

To build them, run:

```r
source("Shiny/build_region_bundles.R")
```

or from the shell:

```sh
Rscript Shiny/build_region_bundles.R
```
